# 项目行为准则与环境规范 (AGENTS.md)

本文件定义了在本仓库（COL-CMOP）中所有 AI 智能体及自动化工具必须遵循的环境、工具调用与开发规范。

---

## 1. Python 环境与包管理约束（核心规则）

- **强制使用 `uv`**：
  - 本项目统一采用 `uv` 进行依赖和虚拟环境管理（根目录包含 `pyproject.toml`、`uv.lock` 及 `.venv`）。
  - **严禁直接调用系统全局的 `python`、`python3` 或 `pip`** 运行任何脚本、测试或安装依赖。
  - 所有 Python 命令必须通过 `uv run` 或项目内激活的虚拟环境执行。

- **模块运行与 Python 路径规范**：
  - 运行任何项目内部脚本或测试文件时，**优先以模块化方式运行**：
    ```bash
    uv run python -m <module_path> [args]
    # 示例：运行单机测试
    uv run python -m tests.run_test --pop-size 20 --max-evals 200
    ```
  - 若必须使用脚本路径执行，**必须显式声明项目根目录到 `PYTHONPATH`**：
    ```bash
    PYTHONPATH=. uv run python tests/run_test.py [args]
    ```
    _严禁直接执行 `python tests/xxx.py`，避免因脚本所在子目录被作为首选搜索路径而导致顶层模块（如 `core`、`algorithms`、`experiments`）导入失败。_

- **依赖变更**：
  - 安装新依赖：`uv add <package>`
  - 开发依赖：`uv add --dev <package>`
  - 同步锁定环境：`uv sync`

---

## 2. 算法实验与测试运行准则

- **轻量验证先行**：
  - 运行或验证演化算法（如 `DSOCOL`、`C1DTLZ1` 等）测试脚本时，默认参数可能包含大量评估次数（如 `FEmax=30000`），在做功能性验证或语法测试时，**必须传入较小的参数进行快速验证**（例如 `--pop-size 20 --max-evals 200`），避免无谓耗时或挂起。
- **单元测试**：
  - 运行单元测试应使用：
    ```bash
    uv run pytest
    ```

---

## 3. Shell 命令执行限制

- **禁止切换目录**：
  - 不得使用 `cd` 改变当前工作目录，所有命令均以项目根目录作为工作路径执行。
- **避免阻塞性长时间命令**：
  - 执行计算密集型或长时间任务时，必须合理设置超时或分步验证，避免长时间无响应。

---

## 4. 代码保护与注释规范

- 保持项目现有的文档和中文注释完整性，严禁随意删除已有的算法推导、数学公式解释或设计说明。
- 遵循现有的代码风格与类型注解规范。
