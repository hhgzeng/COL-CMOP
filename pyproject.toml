[project]
name = "col-cmop"
version = "0.1.0"
description = "COL-CMOP 约束多目标优化算法复现"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "pandas>=2.3.3",
    "pymoo>=0.6.2",
    "pytest>=9.1.1",
]
